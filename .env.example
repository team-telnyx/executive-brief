# Tableau — Personal Access Token (each CSM needs their own)
TABLEAU_PAT_SECRET=your-tableau-pat-secret

# Zendesk — API token (each CSM needs their own)
ZENDESK_API_TOKEN=your-zendesk-api-token

# Slack (optional, for posting briefs to channels)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Config path (optional, defaults to ./config/config.json)
CONFIG_PATH=./config/config.json
